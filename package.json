{
  "name": "@jib/prompt",
  "version": "0.1.0",
  "scripts": {
    "build": "tsc",
    "build:watch": "npm run build -- -w",
    "test": "ts-node src",
    "lint": "tslint -p .",
    "prebuild": "rimraf build",
    "prepack": "npm run build"
  },
  "license": "MIT",
  "files": [
    "build",
    "!build/spec"
  ],
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "description": "inquirer Plugin for @jib/cli",
  "peerDependencies": {
    "@jib/cli": "^0.1.2"
  },
  "devDependencies": {
    "@jib/cli": "^0.1.7",
    "@jib/tslint": "0.0.1",
    "@types/node": "^10.17.28",
    "rimraf": "^2.7.1",
    "ts-node": "^7.0.1",
    "tslint": "^5.20.1",
    "typescript": "^3.9.7"
  },
  "dependencies": {
    "@types/inquirer": "0.0.43",
    "inquirer": "^6.5.2"
  }
}
